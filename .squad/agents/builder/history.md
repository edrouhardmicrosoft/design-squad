# Builder — History

> Implementation and build log.

## Session Log

_(No sessions yet — Builder is ready for deployment.)_

## Learnings

- For Vite demos, gate dev-only overlays with `import.meta.env.DEV` so tooling UI does not render in production builds.
- `bun run dev:all` successfully starts both Vite (`:5173`) and Agentation MCP (`:4747`) for local annotation testing.
